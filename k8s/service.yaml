apiVersion: v1
kind: Service
metadata:
  name: luxemarket-api-service
spec:
  type: NodePort
  selector:
    app: luxemarket-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 32001 # This exposes your app on port 32001 of your KinD node
